This is the original single-scene unit circle lesson. Browser and CLI regression
tests keep using it to verify that existing manifests, scripts, scene contracts,
and assistant configuration still work as authored. The example under
`lessons/unit-circle/` now demonstrates multiple scenes.
